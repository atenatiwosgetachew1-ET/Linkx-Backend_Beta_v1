# PostgreSQL Migrations

 is executed by the official PostgreSQL container only when a new data directory is created.

For existing databases, place ordered migration SQL files here and apply them explicitly with  once that script is connected to your deployment credentials.
